# Cloudflare Worker config for NEXT AI (Workers AI edition — free)
name = "nextafrica-ai-chat"
main = "worker.js"
compatibility_date = "2025-01-01"

# Workers AI binding — gives env.AI access to Cloudflare's hosted models.
# No API key needed, no external account, generous free tier.
[ai]
binding = "AI"
